name: report example secret

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  report:
    runs-on: ubuntu-latest
    env:
      EXAMPLE: ${{ secrets.EXAMPLE }}
    steps:
      - name: Report trigger context
        run: |
          echo "actor=$GITHUB_ACTOR"
          echo "event=$GITHUB_EVENT_NAME"
          echo "pr_author=${{ github.event.pull_request.user.login }}"

      - name: Report the hash of the EXAMPLE environment variable
        run: |
          echo "{$EXAMPLE}" | sha256sum
